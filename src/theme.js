import { extendTheme } from '@chakra-ui/react';

const theme = extendTheme({
  colors: {
    whatsapp: {
      500: '#128C7E',
      600: '#075E54',
      700: '#054D44'
    }
  }
});

export default theme;